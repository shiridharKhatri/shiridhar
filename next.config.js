/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["img.icons8.com"],
    unoptimized: true,
  },
  // basePath: '/github-pages',
};

module.exports = nextConfig;
